from graphex.model import DAGData, DagStepData


def validate(dag_data):
    # TODO: validate dag outputs are reachable
    # TODO: validate unneeded steps (warning)
    # TODO: validate unused inputs (warning)
    pass
